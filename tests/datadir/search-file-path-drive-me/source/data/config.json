{
  "authorization": {
    "oauth_api": {
      "credentials": {
        "#data": "${OAUTH_DATA}",
        "#appSecret": "${OAUTH_APP_SECRET}",
        "appKey": "${OAUTH_APP_ID}"
      }
    }
  },
  "action": "search",
  "parameters": {
    "workbook": {
      "path": "${FIXTURES_CATALOG::ME_DRIVE::FILE_ONE_SHEET::getPath}"
    }
  }
}
