{
  "authorization": {
    "oauth_api": {
      "credentials": {
        "#data": "${OAUTH_DATA}",
        "#appSecret": "${OAUTH_APP_SECRET}",
        "appKey": "${OAUTH_APP_ID}"
      }
    }
  },
  "action": "search",
  "parameters": {
    "workbook": {
      "path": "https://example.com/a/b/c/not/found"
    }
  }
}
