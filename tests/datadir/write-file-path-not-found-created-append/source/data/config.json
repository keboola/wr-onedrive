{
  "authorization": {
    "oauth_api": {
      "credentials": {
        "#data": "${OAUTH_DATA}",
        "#appSecret": "${OAUTH_APP_SECRET}",
        "appKey": "${OAUTH_APP_ID}"
      }
    }
  },
  "parameters": {
    "append": false,
    "workbook": {
      "path": "${FIXTURES_CATALOG::ME_DRIVE::CREATE_TMP_FILE_PATH}"
    },
    "worksheet": {
      "name": "Sheet Test Name",
      "position": 0
    }
  }
}
